import { CheckpipesPipe } from './checkpipes.pipe';

describe('CheckpipesPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckpipesPipe();
    expect(pipe).toBeTruthy();
  });
});
